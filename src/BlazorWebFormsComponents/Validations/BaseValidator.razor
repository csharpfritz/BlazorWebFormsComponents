@inherits BaseStyledComponent
@typeparam Type

@if (Enabled && Visible)
{
	<span id="@ClientID" class="@CssClass" style="@($"{DisplayStyle}{CalculatedStyle}")" title="@ToolTip">
		@if (string.IsNullOrWhiteSpace(Text))
		{
			@ErrorMessage
		}
		else
		{
			@Text
		}
	</span>
}
